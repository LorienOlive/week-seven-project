<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Stat Tracker - Home</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
    <header class="home-header">
      <nav>
        <ul class="nav-row">
          <li class="nav-item">
            <div class="active-user-pic">
            </div>
            {{#displayname}}
            <div class="displayname">
            {{displayname}}
            </div>
            {{/displayname}}
          </li>
          <li class="nav-item">
            <a href="/logout" class="logout-button">Logout</a>
          </li class="nav-item">
            <div class="logo"></div>
            <span class="gabble-top">Stat Tracker</span>
          <li>
        </ul>
      </nav>
    </header>
    <div class="body-wrapper">
      <form action="/home" method="post" class="makeActivity">
        <label for="activity">Enter a new activity</label>
        <input type="text" name="activity" placeholder="Enter an Activity">
        <input type="submit" class="button">
      </form>

      <div class="activity-wrapper">
        <ul class="activity-list">
          {{#activity}}
          <li class="activity"><a href="/activities/{{id}}" class="activity-link">{{name}}</a>
            <form action="/home" method="post" class="delete-button">
              <button type="submit" class="deleteButton" name="deleteButton" value="{{id}}">Delete</button>
            </form>
          </li>
          {{/activity}}
        </ul>
      </div>

    </div>
    <footer>
      <div class="logo"></div>
      <div class="statTracker-bottom"></div>
    </footer>
  </body>
</html>
